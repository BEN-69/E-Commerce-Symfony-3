<form action="{{ path('page_rechercheTraitement') }}" method="post" class="navbar-form form-search pull-right">
    {{ form_widget(form.recherche) }}
    <button type="submit" class="btn">Rechercher</button>
    {{ form_widget(form) }}
</form>